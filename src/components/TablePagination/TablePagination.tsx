import TablePaginationM, { TablePaginationProps } from '@mui/material/TablePagination';

function TablePagination(props: TablePaginationProps) {
  return <TablePaginationM {...props} />;
}

export default TablePagination;
